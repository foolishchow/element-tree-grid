'use strict';

var syntax = require('postcss-scss');
var path = require('path');
var fs = require('fs');
var scssfile = fs.readFile(path.resolve(__dirname, './test/test.scss'));

postcss(plugins).process(scssfile, { syntax: syntax }).then(function (result) {
    console.log(result.content); // SCSS with transformations
});